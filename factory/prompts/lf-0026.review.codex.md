Review Loop Factory spec `lf-0026` against current working tree.

        Native adapter: `codex`
        Spec path: `/Users/omar/Documents/Projects/UIdetox/factory/specs/active/lf-0026-contract-native-operation-remediation.md`

        Review stance:
        - Findings first. Focus correctness, regressions, tests, security, maintainability.
        - Compare implementation against acceptance criteria.
        - Run or inspect verification evidence:
        - `.venv/bin/python -m pytest -q -W error tests/test_source_facts.py tests/test_project_mapping.py tests/test_frontend_mapping.py tests/test_experience_states.py tests/test_redesign_planning.py`
- `.venv/bin/python -m pytest -q -W error`
- `.venv/bin/ruff check .`
- `.venv/bin/ruff format --check .`
- `cd examples/fullstack-slop-lab && npm run build && npm run test:api && npm run test:e2e`
        - If accepted, say `ACCEPTED`.
        - If not accepted, say `CHANGES_REQUESTED` and list blocking items.
        - Do not move files. Operator or CLI archive step moves accepted specs.

        Spec:
        ---
        # Grill Gate

- Owner: UIdetox maintainer/operator; execution authority reaffirmed in the current session.
- Problem: exact backend operation evidence exists, but UI remediation must consume wire/status/media/security/schema applicability without generic cache/retry advice or a parallel contract/state model.
- Out of scope: new contract graph, DTO schema, state vocabulary, cache, retry executor, persistence model, command, compatibility wrapper, target backend behavior change, Figma, runtime-status changes, or map-freshness changes.
- Review failure: public signatures/errors/serialization/order drift; distinct operations/media/statuses flatten; applicability is guessed; lifecycle vocabulary grows; source ownership is lost; old artifacts fail; budgets silently truncate must-preserve evidence; generic retry/cache/idempotency advice appears.
- Riskiest assumption: existing adapters, evidence lattice, graph vocabulary, canonical findings, and lifecycle states are deep enough to carry exact operation obligations without a schema change.
- Smallest acceptable: preserve exact selected-operation evidence through adapter → graph → map → redesign → prototype, emit only applicability-proven canonical obligations, and fail closed on unknown/contradictory/budget-exceeded evidence.

# Context

Execute `plans/059-contract-native-interaction-remediation.md` against Plan 057 and Plan 058 final state at `2ef1cb4`. Reuse `source_facts`, semantic/OpenAPI adapters, canonical contract graph, stable `ProjectMap` facade, native frontend ownership, canonical findings, existing lifecycle states, redesign, prototype, and workflow readiness.

# Acceptance Criteria

- Exact OAS version/dialect, operation identity, parameter location/style/explode, request/response media/status/header/link, security alternative, schema composition/direction/nullability/default/format/bound/reference provenance remain distinct and deterministic.
- Exact selected operation is compared to frontend request/response/error behavior, serialization, validation association, DTO transformation, pagination/filter/sort/order, auth/rate-limit/timeout/cancel/stale/partial/conflict applicability, and affected reads.
- Existing lifecycle states express applicable loading/empty/error/success/disabled/first-run/validation/auth/forbidden/rate-limit/timeout/conflict/partial/stale behavior; no new state is added.
- Mutation remediation covers duplicate-submit, cancellation/recovery, retry, idempotency, optimistic rollback, precondition conflict, atomic/partial outcomes, and affected-read obligations only when exact contract evidence proves applicability.
- Canonical findings retain source and contract anchors, evidence basis/applicability, detector-owned constraints, and observable checks through map, redesign, prototype, and workflow.
- Every must-preserve contract and representative read/mutation survives resource budgets; otherwise generation fails closed with bounded diagnostics.
- Old artifacts/signatures/errors/serialization/order/freshness remain exact.
- Cold fixture and live browser qualification, no-op parity, full tests, Ruff, build, wheel/resource parity, diff hygiene, secret/debug/bloat checks, verified patch, and rollback pass.

# Constraints

- Work one contract family per RED → GREEN → review → regression → commit slice.
- Preserve intentional service/DTO transformations and authorization filtering; database schema is not automatically UI schema.
- Never emit generic add-cache/add-retry/add-idempotency guidance.
- Do not add a second graph/schema/cache/state/command/wrapper.
- Stop rather than weaken a fail-closed boundary.

# Review Notes

- Freeze the current facade and old-artifact behavior before implementation.
- Test order reversal, duplicate/reordered operation, same path/different method, same status/different media, recursive/oversized/hostile schema, security alternatives, safe/unsafe retry, missing/present idempotency, 412/conflict, partial batch, duplicate submit, rollback, and affected reads.
- Review exact source ownership and contract applicability in generated redesign/prototype output.
